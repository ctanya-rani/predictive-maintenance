"""TrainWatch — predictive maintenance on synthetic train sensor data.

Pipeline: generate synthetic fleet telemetry -> detect anomalies ->
raise alerts -> render an HTML dashboard.
"""

__version__ = "0.1.0"
